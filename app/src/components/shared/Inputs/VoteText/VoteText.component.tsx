import React, { FunctionComponent } from 'react';

import './style.sass';

export const VoteText: FunctionComponent<{}> = ({}) => {
    return (
        <>
            <p>VoteText</p>
        </>
    );
}

