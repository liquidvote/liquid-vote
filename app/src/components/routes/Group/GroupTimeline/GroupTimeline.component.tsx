import React, { FunctionComponent } from 'react';

import './style.sass';

export const GroupTimeline: FunctionComponent<{ selectedChannels: any }> = ({ selectedChannels }) => {
    return (
        <>
            <p>GroupTimeline</p>
        </>
    );
}

